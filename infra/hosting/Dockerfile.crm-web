FROM node:22-bookworm-slim
WORKDIR /app
COPY apps/crm-web/package*.json ./
RUN npm ci
COPY apps/crm-web/ ./
ENV NEXT_PUBLIC_CRM_WEB_MODE=bff
ENV NEXT_PUBLIC_CRM_BFF_URL=/api/crm
RUN npm run build && npm prune --omit=dev
ENV NODE_ENV=production
EXPOSE 3000
CMD ["npm", "run", "start"]
