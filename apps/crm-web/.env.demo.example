# Copiar a apps/crm-web/.env.local para la demo remota detras del proxy.
# La URL es relativa a proposito: el front y el BFF quedan en el mismo origen.
NEXT_PUBLIC_CRM_WEB_MODE=bff
NEXT_PUBLIC_CRM_BFF_URL=/api/crm
